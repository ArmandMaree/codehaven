{
  "name": "codehaven",
  "version": "0.1.0",
  "description": "",
  "main": "server/dist/index.js",
  "scripts": {
    "clean": "rimraf dist/*",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "tsc": "tsc",
    "build": "npm-run-all clean lint tsc && cd client && npm install && npm run build",
    "litebuild": "npm-run-all clean lint tsc",
    "start": "node .",
    "build:start": "npm-run-all build start",
    "litebuild:start": "npm-run-all litebuild start",
    "dev": "nodemon --watch server/src,client/src -e ts,ejs,js --exec npm run dev:start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Armand Maree <aamaree@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "rsuite": "^4.10.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.12",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "tsc": "^2.0.3",
    "tslint": "^6.1.3",
    "typescript": "^4.4.2"
  }
}
